# Notes CLI

Простой CLI для управления заметками на Java.

## Команды

- **add** — добавить заметку
- **list** — показать все заметки
- **rm** — удалить заметку по id

### Примеры

Добавить заметку:

```bash
java -cp src com.example.App --cmd=add --text="Купить хлеб"
